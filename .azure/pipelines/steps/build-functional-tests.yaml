#
# Build functional tests
#
# Needed to generate the playwright.ps1 file so we can install browsers
#

steps:
- task: DotNetCoreCLI@2
  displayName: 'Build functional tests'
  inputs:
    command: 'build'
    workingDirectory: $(Solution.FunctionalTestsDirectory)
    