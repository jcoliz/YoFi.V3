#
# Use the .NET SDK
#

parameters:
- name: version
  type: string 

steps:
- task: UseDotNet@2
  displayName: 'Use .NET ${{ parameters.version }} SDK'
  inputs:
    version: ${{ parameters.version }}

- script: 'dotnet --version'
  displayName: 'Check dotnet version'
