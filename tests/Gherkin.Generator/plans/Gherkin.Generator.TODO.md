# Bugs/fixes!

## 1.0.1

* Doesn't seem to include the table in the args for datatable items
* Need to output the whole CRIF for debugging.
* Add "FeatureFile", and then we can use that for the symbolic name of the class
* Parsing errors need to give details
